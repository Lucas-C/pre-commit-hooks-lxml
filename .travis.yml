language: python
python:
  - "3.5"
  - "3.6"
  - "3.7"
# command to install dependencies
install:
  - pip install .
  - pip install -r dev-requirements.txt
# command to run tests
script: pre-commit install && pre-commit run --all-files
notifications:
  email:
    on_success: never